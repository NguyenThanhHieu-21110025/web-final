<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .btn-hover:hover {
            transform: scale(1.05);
            transition: transform 0.3s ease-in-out;
        }

        .btn-edit,
        .btn-delete {
            transition: background-color 0.3s ease-in-out;
        }

        .btn-edit {
            background-color: #95bee9;
            border-color: #007bff;
        }

        .btn-edit:hover {
            background-color: #f2e235;
            border-color: #004085;
        }

        .btn-delete {
            background-color: #95bee9;
            border-color: #007bff;
        }

        .btn-delete:hover {
            background-color: #f83917;
            border-color: #004085;
        }
    </style>
</head>

<body class="bg-light">

    <div class="container mt-5">
        <h1 class="mb-4 text-center">{{title}}</h1>

        <table class="table table-striped table-bordered shadow-sm">
            <thead class="table-light">
                <tr>
                    <th scope="col">Tên bài viết</th>
                    <th scope="col">Chuyên mục</th>
                    <th scope="col">Tác giả</th>
                    <th scope="col">Trạng thái</th>
                    <th scope="col" class="text-center">Thao tác</th>
                </tr>
            </thead>
            <tbody>
                {{#each articles}}
                <tr>
                    <td>{{title}}</td> 
                    <td>{{category.category_name}}</td>
                    <td>{{author.username}}</td>
                    <td>{{status}}</td> 
                    <td class="text-center">
                        <a href="/admin/articles/{{_id}}/edit" class="btn btn-sm btn-edit btn-hover">Sửa</a>
                        <form action="/admin/articles/{{_id}}/delete" method="POST" style="display:inline;">
                            <button type="submit" class="btn btn-sm btn-delete btn-hover">Xóa</button>
                        </form>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>