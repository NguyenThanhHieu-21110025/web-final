<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - Electronic Newspaper</title>
    <link rel="stylesheet" href="../../public/css/main.css">
</head>
<body>
    <header>
        <!-- Menu -->
        <nav>
            <ul>
                <li><a href="/guest/home">Home</a></li>
                <li><a href="/guest/category/kinh-doanh-nong-san">Nông Sản</a></li>
                <li><a href="/guest/category/kinh-doanh-hai-san">Hải Sản</a></li>
                <!-- Add more menu items here -->
            </ul>
        </nav>
    </header>

    <main>
        <section class="highlighted-articles">
            <h2>Highlighted Articles</h2>
            {{#each topArticles}}
                <article>
                    <h3>{{this.title}}</h3>
                    <p>Category: {{this.category}}</p>
                    <p>Date: {{this.createdAt}}</p>
                    <img src="{{this.thumbnail}}" alt="{{this.title}}">
                </article>
            {{/each}}
        </section>

        <section class="most-viewed-articles">
            <h2>Most Viewed Articles</h2>
            {{#each mostViewedArticles}}
                <article>
                    <h3>{{this.title}}</h3>
                    <p>Category: {{this.category}}</p>
                    <p>Date: {{this.createdAt}}</p>
                    <img src="{{this.thumbnail}}" alt="{{this.title}}">
                </article>
            {{/each}}
        </section>

        <section class="latest-articles">
            <h2>Latest Articles</h2>
            {{#each latestArticles}}
                <article>
                    <h3>{{this.title}}</h3>
                    <p>Category: {{this.category}}</p>
                    <p>Date: {{this.createdAt}}</p>
                    <img src="{{this.thumbnail}}" alt="{{this.title}}">
                </article>
            {{/each}}
        </section>
    </main>
</body>
</html>
