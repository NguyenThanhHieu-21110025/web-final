<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Article Detail - Electronic Newspaper</title>
    <link rel="stylesheet" href="../../public/css/main.css">
</head>
<body>
    <header>
        <!-- Menu -->
        <nav>
            <ul>
                <li><a href="/guest/home">Home</a></li>
                <li><a href="/guest/category/kinh-doanh-nong-san">Nông Sản</a></li>
                <li><a href="/guest/category/kinh-doanh-hai-san">Hải Sản</a></li>
                <!-- Add more menu items here -->
            </ul>
        </nav>
    </header>

    <main>
        <article>
            <h1>{{article.title}}</h1>
            <p>Category: {{article.category}}</p>
            <p>Date: {{article.createdAt}}</p>
            <img src="{{article.thumbnail}}" alt="{{article.title}}">
            <p>{{article.content}}</p>
            <p>Tags: {{#each article.tags}}<span>{{this}}</span>{{/each}}</p>
        </article>

        <section class="comments">
            <h2>Comments</h2>
            {{#each article.comments}}
                <div class="comment">
                    <p>{{this.date}}</p>
                    <p>{{this.userName}}</p>
                    <p>{{this.content}}</p>
                </div>
            {{/each}}
        </section>

        <section class="random-articles">
            <h2>Related Articles</h2>
            {{#each randomArticles}}
                <article>
                    <h3>{{this.title}}</h3>
                    <p>Category: {{this.category}}</p>
                    <p>Date: {{this.createdAt}}</p>
                    <img src="{{this.thumbnail}}" alt="{{this.title}}">
                </article>
            {{/each}}
        </section>
    </main>
</body>
</html>
