<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tag - Electronic Newspaper</title>
    <link rel="stylesheet" href="../../public/css/main.css">
</head>
<body>
    <header>
        <!-- Menu -->
        <nav>
            <ul>
                <li><a href="/guest/home">Home</a></li>
                <li><a href="/guest/category/kinh-doanh-nong-san">Nông Sản</a></li>
                <li><a href="/guest/category/kinh-doanh-hai-san">Hải Sản</a></li>
                <!-- Add more menu items here -->
            </ul>
        </nav>
    </header>

    <main>
        <section class="tag-articles">
            <h2>Articles with Tag</h2>
            {{#each articles}}
                <article>
                    <h3>{{this.title}}</h3>
                    <p>Category: {{this.category}}</p>
                    <p>Date: {{this.createdAt}}</p>
                    <p>Tags: {{#each this.tags}}<span>{{this}}</span>{{/each}}</p>
                    <img src="{{this.thumbnail}}" alt="{{this.title}}">
                    <p>{{this.abstract}}</p>
                </article>
            {{/each}}
        </section>
    </main>
</body>
</html>
